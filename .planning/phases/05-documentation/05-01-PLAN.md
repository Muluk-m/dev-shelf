---
phase: 05-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]

must_haves:
  truths:
    - "README contains a visible Cloudflare Deploy Button badge that links to one-click deployment"
    - "README includes step-by-step manual deployment instructions using wrangler CLI"
    - "A developer can set up a local development environment by following the README instructions alone"
    - "All environment variables are documented with descriptions, required/optional status, and example values"
  artifacts:
    - path: "README.md"
      provides: "Complete project documentation for deployment, setup, and development"
      min_lines: 100
      contains: "deploy.workers.cloudflare.com/button"
  key_links:
    - from: "README.md"
      to: ".dev.vars.example"
      via: "references as env var template"
      pattern: "\\.dev\\.vars\\.example"
    - from: "README.md"
      to: "wrangler.jsonc"
      via: "references as deployment config"
      pattern: "wrangler"
---

<objective>
Write a comprehensive README.md that enables any developer to deploy and use DevHub with no external documentation.

Purpose: The README is the single entry point for the open-source project. It must cover one-click deployment (Deploy Button), manual deployment (wrangler CLI), local development setup, and environment variable configuration. This completes the documentation requirements for the v1 open-source release.

Output: A complete README.md at the repository root.
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-RESEARCH.md

Key project context for README authoring:
- Deploy Button badge: `[![Deploy to Cloudflare](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=REPO_URL)`
- Package manager: pnpm (pinned via packageManager field)
- Dev command: `pnpm run dev` (starts Vite on port 5173)
- Build/deploy: `pnpm run build && wrangler deploy` (or `pnpm run deploy`)
- Database: Cloudflare D1 with binding "DB"
- Linting: Biome (`pnpm run lint` / `pnpm run lint:fix`)
- Tech stack: React 19, React Router 7, Hono, Tailwind CSS, shadcn/ui, Cloudflare Workers
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write comprehensive README.md</name>
  <files>README.md</files>
  <action>
Create README.md at the repository root with the following sections. Read the current state of these files FIRST to ensure accuracy: wrangler.jsonc (for bindings, vars, project name), package.json (for scripts, packageManager), .dev.vars.example (for env var reference -- created in Phase 4), and any migration files in migrations/ or db/ directory.

**README structure (in order):**

1. **Hero Section**
   - Project name: "DevHub"
   - One-line description: "A self-hosted developer tool management platform, deployed on Cloudflare Workers"
   - Deploy to Cloudflare button badge (DOC-01): `[![Deploy to Cloudflare](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/OWNER/devhub)` -- use actual repo URL if DEPLOY-05 has been completed, otherwise use placeholder with a TODO comment in the markdown source
   - Optional: License badge if LICENSE file exists

2. **Features** (brief bullet list)
   - Tool management with CRUD operations
   - Multi-environment support (Dev/Test/Prod URLs per tool)
   - Command palette (Cmd/Ctrl+K) for keyboard-first navigation
   - Built-in utilities (JSON formatter, Base64 encoder, etc.)
   - Role-based access control (Admin and User roles)
   - First-run setup wizard for zero-config deployment
   - Light/dark theme support
   - Responsive design

3. **Quick Start: One-Click Deploy** (DOC-01)
   - Explain what the Deploy Button does (provisions D1 database, configures Workers)
   - "Click the button above" with a repeated button badge
   - Post-deployment steps: visit deployed URL, complete first-run setup wizard to create admin account, log in and start managing tools
   - Note: you may need to set the JWT_SECRET via `wrangler secret put JWT_SECRET`

4. **Manual Deployment** (DOC-02)
   - Prerequisites: Cloudflare account, Node.js, pnpm, wrangler CLI (`npm install -g wrangler`)
   - Step-by-step with copy-paste commands:
     a. Clone the repository
     b. Install dependencies: `pnpm install`
     c. Create D1 database: `wrangler d1 create devhub-database`
     d. Update wrangler.jsonc with the database_id from the create command output
     e. Apply database migrations: `wrangler d1 execute devhub-database --remote --file=<migration_file>` (use actual migration file path from codebase)
     f. Set secrets: `wrangler secret put JWT_SECRET`
     g. Deploy: `pnpm run deploy`
     h. Visit deployed URL and complete first-run setup

5. **Local Development** (DOC-03)
   - Prerequisites: Node.js (version if specified in engines), pnpm
   - Step-by-step:
     a. Clone and install: `git clone ... && cd devhub && pnpm install`
     b. Copy env file: `cp .dev.vars.example .dev.vars` and fill in values
     c. Set up local database: explain local D1 setup (wrangler d1 execute with --local flag, or the db:apply script if it exists)
     d. Start dev server: `pnpm run dev`
     e. Open http://localhost:5173
   - Other useful commands table: typecheck, lint, lint:fix, build, preview, cf-typegen

6. **Environment Variables** (DOC-04)
   - Table format: | Variable | Description | Required | Default | Example |
   - Document every variable from .dev.vars.example and wrangler.jsonc vars section
   - Include JWT_SECRET (from Phase 2), API_BASE_URL, and any other vars that remain after Phase 1 cleanup
   - Explain difference between vars (in wrangler.jsonc) and secrets (via wrangler secret put)

7. **Tech Stack** (brief)
   - Bullet list: React 19, React Router 7, Hono, Tailwind CSS, shadcn/ui, Cloudflare Workers, D1 Database, pnpm

8. **Project Structure** (high-level)
   - Tree showing key directories: app/ (frontend), workers/ (backend API), lib/ (shared), db/ or migrations/ (database)

9. **License**
   - Reference LICENSE file if it exists, or note "MIT" (standard for open-source)

**Writing guidelines:**
- Use GitHub Flavored Markdown
- Every CLI command should be in a fenced code block with `bash` language tag
- Use tables for structured data (env vars, commands)
- Keep descriptions concise but complete -- developers scan, not read
- No emojis in headings or body text
- Use relative links for referencing project files (e.g., `.dev.vars.example`)
- Ensure all referenced files actually exist in the codebase at execution time
  </action>
  <verify>
    <automated>test -f README.md && grep -q "deploy.workers.cloudflare.com/button" README.md && grep -q "wrangler" README.md && grep -q "pnpm run dev" README.md && grep -q "JWT_SECRET\|Environment" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
README.md exists at repo root with all four documentation requirements covered:
- Deploy Button badge visible at top (DOC-01)
- Manual deployment section with wrangler CLI steps (DOC-02)
- Local development setup section with step-by-step instructions (DOC-03)
- Environment variables table with descriptions and examples (DOC-04)
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate README completeness and links</name>
  <files>README.md</files>
  <action>
Verify the README created in Task 1 meets all requirements by checking:

1. **DOC-01 check:** Confirm the Deploy Button badge renders correctly (the markdown contains the correct image URL `https://deploy.workers.cloudflare.com/button` and links to `https://deploy.workers.cloudflare.com/?url=...`)

2. **DOC-02 check:** Confirm manual deployment section includes all steps: clone, install, create D1 database, update config, run migrations, set secrets, deploy. Each step has an executable command.

3. **DOC-03 check:** Confirm local development section includes: prerequisites, clone, install, env file copy, database setup, dev server start command, and the dev server URL.

4. **DOC-04 check:** Confirm environment variables section has a table with at least these columns: Variable name, Description, Required/Optional. Every variable from .dev.vars.example is listed.

5. **File reference check:** Verify that every file referenced in the README (e.g., .dev.vars.example, wrangler.jsonc, migration files) actually exists in the codebase. Fix any broken references.

6. **Command check:** Verify that every pnpm/wrangler command mentioned in the README matches what's in package.json scripts and is a valid command.

If any issues found, fix them in README.md directly.
  </action>
  <verify>
    <automated>grep -c "##" README.md | xargs -I{} test {} -ge 6 && grep -q "deploy.workers.cloudflare.com" README.md && grep -q "wrangler d1" README.md && grep -q "pnpm install" README.md && grep -q "pnpm run dev" README.md && echo "ALL CHECKS PASS" || echo "CHECKS FAILED"</automated>
  </verify>
  <done>
README.md passes all validation checks:
- Contains Deploy Button with correct URLs
- Manual deployment section has complete step-by-step commands
- Local dev section has complete setup instructions
- Environment variables are fully documented
- All file references point to existing files
- All commands match package.json scripts
  </done>
</task>

</tasks>

<verification>
Phase 5 overall verification:

1. `grep -q "deploy.workers.cloudflare.com/button" README.md` -- Deploy Button badge exists (DOC-01)
2. `grep -q "wrangler deploy\|pnpm run deploy" README.md` -- Manual deployment documented (DOC-02)
3. `grep -q "pnpm run dev" README.md` -- Local dev instructions present (DOC-03)
4. `grep -c "|" README.md` should show multiple table rows for env var documentation (DOC-04)
5. `wc -l README.md` should show 100+ lines (comprehensive documentation)
</verification>

<success_criteria>
- README.md exists at repository root with 100+ lines of content
- The Cloudflare Deploy Button badge is visible in the hero section
- A developer with no prior knowledge can follow the manual deployment steps to deploy via wrangler
- A developer can set up a local development environment by following the README alone
- All environment variables are documented with descriptions and example values
- All referenced files and commands are valid
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-01-SUMMARY.md`
</output>
