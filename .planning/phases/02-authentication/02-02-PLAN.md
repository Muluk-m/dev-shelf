---
phase: 02-authentication
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/types/user-info.ts
  - app/stores/user-info-store.ts
  - app/hooks/use-permissions.ts
  - app/lib/api.ts
  - app/routes/login.tsx
  - app/routes/register.tsx
  - app/routes/settings.tsx
  - app/routes.ts
  - app/components/user-profile.tsx
  - app/components/auth/login-form.tsx
  - app/components/auth/register-form.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-05, AUTH-06, AUTH-07]

must_haves:
  truths:
    - "User can navigate to /login and see a login form with username and password fields"
    - "User can navigate to /register and see a registration form"
    - "User can register with username/password and is automatically logged in"
    - "User can log in with valid credentials and is redirected to home page"
    - "User can log out from any page via the user profile dropdown"
    - "User can change password from /settings page"
    - "User can update display name from /settings page"
    - "Login state persists across page refresh (JWT cookie)"
  artifacts:
    - path: "app/routes/login.tsx"
      provides: "Login page with form"
      min_lines: 30
    - path: "app/routes/register.tsx"
      provides: "Registration page with form"
      min_lines: 30
    - path: "app/routes/settings.tsx"
      provides: "Personal settings page with password change and profile update"
      min_lines: 50
    - path: "app/components/auth/login-form.tsx"
      provides: "Login form component"
      exports: ["LoginForm"]
    - path: "app/components/auth/register-form.tsx"
      provides: "Register form component"
      exports: ["RegisterForm"]
    - path: "app/types/user-info.ts"
      provides: "Updated user info type for local auth"
      exports: ["UserInfo"]
    - path: "app/stores/user-info-store.ts"
      provides: "Auth state management"
      exports: ["useUserInfoStore"]
    - path: "app/lib/api.ts"
      provides: "Auth API client functions"
      exports: ["login", "register", "logout", "getUserInfo", "changePassword", "updateProfile"]
  key_links:
    - from: "app/components/auth/login-form.tsx"
      to: "app/lib/api.ts"
      via: "login() API call on form submit"
      pattern: "login.*username.*password"
    - from: "app/stores/user-info-store.ts"
      to: "app/lib/api.ts"
      via: "getUserInfo() to refresh user state"
      pattern: "getUserInfo"
    - from: "app/routes/settings.tsx"
      to: "app/lib/api.ts"
      via: "changePassword() and updateProfile() calls"
      pattern: "changePassword|updateProfile"
    - from: "app/components/user-profile.tsx"
      to: "app/stores/user-info-store.ts"
      via: "useUserInfoStore() for user state and logout action"
      pattern: "useUserInfoStore"
    - from: "app/routes.ts"
      to: "app/routes/login.tsx"
      via: "route definition for /login"
      pattern: 'route.*login'
---

<objective>
Build the frontend authentication UI: login page, registration page, personal settings page (password change and display name update), and update the user profile component, auth stores, and types to work with the new local auth backend.

Purpose: Provide the user-facing interface for the authentication system built in Plan 02-01. Users need to be able to register, log in, manage their profile, and the UI must reflect auth state correctly.
Output: Working login/register/settings pages, updated user profile dropdown with logout, auth state management via Zustand.
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-01-SUMMARY.md

@app/root.tsx
@app/routes.ts
@app/lib/api.ts
@app/types/user-info.ts
@app/stores/user-info-store.ts
@app/hooks/use-permissions.ts
@app/components/user-profile.tsx
@app/components/layout/header.tsx
@app/components/ui/button.tsx
@app/components/ui/input.tsx
@app/components/ui/label.tsx

<interfaces>
<!-- From Plan 02-01 backend (these types will exist after Plan 01 executes) -->

From lib/types/auth.ts:
```typescript
export interface User {
  id: string;
  username: string;
  displayName: string;
  passwordHash: string;
  role: 'admin' | 'user';
  createdAt: string;
  updatedAt: string;
}

export type PublicUser = Omit<User, 'passwordHash'>;

export interface LoginRequest {
  username: string;
  password: string;
}

export interface RegisterRequest {
  username: string;
  password: string;
  displayName?: string;
}

export interface ChangePasswordRequest {
  currentPassword: string;
  newPassword: string;
}

export interface UpdateProfileRequest {
  displayName: string;
}
```

From workers/routes/auth.ts (API contract):
```
POST /api/auth/register  { username, password, displayName? } → 201 { user: PublicUser }
POST /api/auth/login     { username, password }                → 200 { user: PublicUser }
POST /api/auth/logout                                          → 200 { message: string }
GET  /api/auth/me                                              → 200 { user: PublicUser } | 401
```

Existing UI components available:
- shadcn/ui: Button, Input, Label, Card, Tabs, Avatar, DropdownMenu, Separator, Sonner (toast)
- Tailwind CSS 4.1 with utility classes
- Lucide React icons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update auth types, API client, and auth store for local auth</name>
  <files>app/types/user-info.ts, app/lib/api.ts, app/stores/user-info-store.ts, app/hooks/use-permissions.ts</files>
  <action>
1. **Rewrite `app/types/user-info.ts`** — replace Feishu-specific fields with local auth model:
```typescript
export interface UserInfo {
  id: string;
  username: string;
  displayName: string;
  role: 'admin' | 'user';
}
```
Remove: userId (number), openId, avatar, platform, appId, isNewUser, iat, exp, aud, iss, roles[], permissions[].

2. **Update `app/lib/api.ts`** — replace auth-related API functions. Keep tool/category functions unchanged.

Replace the API_BASE_URL to be more generic (remove hardcoded qiliangjia.one domain):
```typescript
export const API_BASE_URL = import.meta.env.DEV
  ? "http://localhost:5173"
  : "";  // Empty string = same origin in production
```

Replace/add auth functions:
- `login(username: string, password: string): Promise<{ user: UserInfo }>` — POST /api/auth/login with credentials: 'include'
- `register(username: string, password: string, displayName?: string): Promise<{ user: UserInfo }>` — POST /api/auth/register with credentials: 'include'
- `logout(): Promise<void>` — POST /api/auth/logout with credentials: 'include'
- `getUserInfo(): Promise<{ user: UserInfo }>` — GET /api/auth/me with credentials: 'include'
- `changePassword(currentPassword: string, newPassword: string): Promise<{ message: string }>` — POST /api/auth/change-password with credentials: 'include'
- `updateProfile(displayName: string): Promise<{ user: UserInfo }>` — PUT /api/auth/profile with credentials: 'include'

Important: All fetch calls to auth endpoints MUST include `credentials: 'include'` so cookies are sent. Also add `credentials: 'include'` to existing tool API calls that use POST/PUT/DELETE (they need auth too).

Remove: All Feishu-specific API functions, CF logs functions, query analyzer functions, icon generator functions, AB router functions — these should be removed in Phase 1; clean up any remaining ones.

3. **Update `app/stores/user-info-store.ts`** — adapt to new UserInfo type:
- Keep the same store structure (Zustand with persist)
- Update `refresh()` to call the new `getUserInfo()` which returns `{ user: UserInfo }`
- Update `logout()` to call the new `logout()` API function, clear store, redirect to /login
- The `userInfo` field type changes from Feishu UserInfo to the new simple UserInfo
- Keep localStorage persistence for userInfo
- Keep the onRehydrateStorage pattern that auto-refreshes on mount

4. **Simplify `app/hooks/use-permissions.ts`** — adapt to simple admin/user role model:
- The new UserInfo has a single `role` field ('admin' | 'user') instead of roles[]
- Simplify `hasRole` to compare against single role with hierarchy: admin > user
- Keep `isAdmin` check: `userInfo?.role === 'admin'`
- Remove permission-based checks (hasPermission) — Phase 3 will reintroduce granular RBAC if needed
- Keep the hook API compatible: `{ userInfo, roles: [role], isAdmin, hasRole, loading }`
  </action>
  <verify>
    <automated>cd /Users/qian/Projects/Qlj/qlj-devhub-homepage && grep -q "username" app/types/user-info.ts && grep -q "displayName" app/types/user-info.ts && grep -q "login" app/lib/api.ts && grep -q "register" app/lib/api.ts && grep -q "changePassword" app/lib/api.ts && grep -q "credentials" app/lib/api.ts && grep -q "role" app/hooks/use-permissions.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- UserInfo type has id, username, displayName, role fields (no Feishu fields)
- API client has login, register, logout, getUserInfo, changePassword, updateProfile functions
- All auth API calls include credentials: 'include' for cookie handling
- User info store works with new UserInfo type
- Permissions hook simplified for admin/user role model
- API_BASE_URL uses empty string for production (same-origin)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create login and register pages with form components</name>
  <files>app/components/auth/login-form.tsx, app/components/auth/register-form.tsx, app/routes/login.tsx, app/routes/register.tsx, app/routes.ts</files>
  <action>
1. **Create `app/components/auth/login-form.tsx`** — LoginForm component:
- Form with username (Input) and password (Input type="password") fields
- Uses shadcn/ui components: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, Button, Input, Label
- Client-side validation: username required, password required (min 8 chars)
- Submit handler calls `login()` from api.ts
- On success: update userInfoStore with returned user, navigate to "/" (or redirectTo query param)
- On error: show error message inline (red text below form), handle 401 specifically with "Invalid credentials" message
- Loading state on submit button (disable + spinner text)
- Link to register page below form: "Don't have an account? Register"
- Centered layout with max-w-md, suitable for full-page display

2. **Create `app/components/auth/register-form.tsx`** — RegisterForm component:
- Form with username, display name (optional), password, and confirm password fields
- Client-side validation: username 3-50 chars (alphanumeric + underscore), password min 8 chars, confirm password must match
- Submit handler calls `register()` from api.ts
- On success: update userInfoStore with returned user, navigate to "/"
- On error: show error message inline, handle 409 specifically with "Username already taken" message
- Loading state on submit button
- Link to login page below form: "Already have an account? Login"
- Same centered layout as login form

3. **Create `app/routes/login.tsx`** — Login page route:
- Simple page component that renders LoginForm centered on screen
- Add meta title: "Login - DevTools"
- If user is already logged in (check userInfoStore), redirect to "/"
- Minimal layout: no Header, just the form centered vertically and horizontally with subtle background

4. **Create `app/routes/register.tsx`** — Register page route:
- Simple page component that renders RegisterForm centered on screen
- Add meta title: "Register - DevTools"
- If user is already logged in, redirect to "/"
- Same minimal layout as login page

5. **Update `app/routes.ts`** — add new routes:
```typescript
route("login", "routes/login.tsx"),
route("register", "routes/register.tsx"),
```
Add these AFTER the index route but before the tools routes.

Design notes:
- Use the existing theme (light/dark) — forms should respect the current theme
- Use Sonner toast for success notifications ("Logged in successfully", "Account created")
- Use `useNavigate` from react-router for navigation after auth actions
- The forms should look clean and professional, matching the existing shadcn/ui design system
- Use Lucide icons: User for username field, Lock for password field
  </action>
  <verify>
    <automated>cd /Users/qian/Projects/Qlj/qlj-devhub-homepage && test -f app/components/auth/login-form.tsx && test -f app/components/auth/register-form.tsx && test -f app/routes/login.tsx && test -f app/routes/register.tsx && grep -q "login" app/routes.ts && grep -q "register" app/routes.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- Login page at /login with username/password form
- Register page at /register with username/password/confirm form
- Forms use shadcn/ui components (Card, Input, Button, Label)
- Client-side validation (required fields, password match, username format)
- Error messages displayed inline for failed attempts
- Success redirects to home page
- Links between login and register pages
- Routes registered in app/routes.ts
  </done>
</task>

<task type="auto">
  <name>Task 3: Create settings page and update user profile component</name>
  <files>app/routes/settings.tsx, app/components/user-profile.tsx, app/routes.ts</files>
  <action>
1. **Create `app/routes/settings.tsx`** — Personal settings page:
- Page with two sections in a clean card layout:

**Section 1: Profile Information**
- Display current username (read-only, shown as text)
- Display name field (editable Input)
- Role badge (shown as text, not editable)
- "Save Changes" button that calls updateProfile()
- Show success toast on save, error message on failure

**Section 2: Change Password**
- Current password field (Input type="password")
- New password field (Input type="password")
- Confirm new password field (Input type="password")
- Validation: current password required, new password min 8 chars, confirm must match
- "Change Password" button that calls changePassword()
- Show success toast on change, specific error for wrong current password
- Clear form after successful change

Layout:
- Page title: "Personal Settings"
- Use the main app layout with Header (import Header from layout)
- Content area max-w-2xl centered
- Two Card components stacked vertically with gap
- If user is not logged in, redirect to /login
- Add meta title: "Settings - DevTools"

Use shadcn/ui: Card, CardHeader, CardTitle, CardDescription, CardContent, Button, Input, Label, Separator, Badge (for role)
Use Sonner toast for success/error notifications

2. **Update `app/components/user-profile.tsx`** — adapt to new auth model:
- Remove Feishu-specific UI (platform display showing "飞书", avatar from Feishu)
- Show username and display name in dropdown header
- Show role as subtitle (e.g., "Admin" or "User")
- Replace avatar: use initials from displayName (existing initials logic), remove AvatarImage (no avatar URL in new model)
- Update "Login" link: change `href="/auth/login"` to `href="/login"`
- Add Settings menu item that navigates to "/settings" (currently disabled in old code — enable it)
- Keep Logout menu item, update to use new logout from store
- Use `useNavigate` from react-router for Settings navigation

3. **Update `app/routes.ts`** — add settings route:
```typescript
route("settings", "routes/settings.tsx"),
```

4. **Verify build** — run `pnpm run build` to ensure everything compiles.
  </action>
  <verify>
    <automated>cd /Users/qian/Projects/Qlj/qlj-devhub-homepage && test -f app/routes/settings.tsx && grep -q "settings" app/routes.ts && grep -q "displayName" app/components/user-profile.tsx && grep -q '"/login"' app/components/user-profile.tsx && grep -q '"/settings"' app/components/user-profile.tsx && pnpm run build 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- Settings page at /settings with profile update and password change sections
- Profile section shows username (read-only), editable display name, role badge
- Password change section validates current password and new password match
- User profile dropdown shows username, display name, role
- Settings link enabled in user profile dropdown (navigates to /settings)
- Login link updated to /login (not /auth/login)
- Logout works via new auth API
- Application builds successfully with `pnpm run build`
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Build check:** `pnpm run build` succeeds with no TypeScript errors
2. **Login page:** /login route renders a form with username and password fields
3. **Register page:** /register route renders a form with username, display name, password, confirm password
4. **Settings page:** /settings route renders profile update and password change sections
5. **User profile:** Dropdown shows username/displayName/role, has Settings and Logout options
6. **Auth flow:** Login form submits to /api/auth/login, register form to /api/auth/register
7. **Store updated:** userInfoStore uses new UserInfo type with id, username, displayName, role
8. **No Feishu references:** No remaining references to openId, feishuId, platform: "feishu", avatar from Feishu
</verification>

<success_criteria>
- `pnpm run build` succeeds
- /login, /register, /settings routes exist and render properly
- Login form calls POST /api/auth/login with credentials: 'include'
- Register form calls POST /api/auth/register with credentials: 'include'
- Settings page allows password change and display name update
- User profile dropdown shows correct auth state and has working logout
- All auth state persisted via Zustand localStorage (survives page refresh)
- No Feishu-specific code remains in frontend auth files
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-02-SUMMARY.md`
</output>
