---
phase: 04-deployment-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wrangler.jsonc
  - package.json
  - .gitignore
  - .dev.vars.example
  - migrations/0001_initial_schema.sql
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04

must_haves:
  truths:
    - "wrangler.jsonc contains D1 and KV binding definitions with empty IDs for auto-provisioning"
    - "migrations/ directory contains numbered SQL files that create all application tables"
    - "deploy script in package.json chains migration application before wrangler deploy"
    - ".dev.vars.example documents all required secrets with generation instructions"
    - ".dev.vars.example is not caught by .gitignore pattern"
  artifacts:
    - path: "wrangler.jsonc"
      provides: "Clean Deploy Button configuration with empty resource IDs"
      contains: "database_id.*\"\""
    - path: "migrations/0001_initial_schema.sql"
      provides: "Initial database schema migration"
      contains: "CREATE TABLE"
    - path: ".dev.vars.example"
      provides: "Secret template for deployment"
      contains: "JWT_SECRET"
    - path: "package.json"
      provides: "Deploy script with migration chaining and binding descriptions"
      contains: "migrations apply"
    - path: ".gitignore"
      provides: "Updated gitignore that tracks .dev.vars.example"
      contains: "!.dev.vars.example"
  key_links:
    - from: "package.json deploy script"
      to: "migrations/0001_initial_schema.sql"
      via: "wrangler d1 migrations apply DB --remote"
      pattern: "migrations apply DB"
    - from: "wrangler.jsonc d1_databases"
      to: "migrations/"
      via: "migrations_dir configuration"
      pattern: "migrations_dir"
---

<objective>
Configure the project's deployment infrastructure so Cloudflare's Deploy Button can auto-provision D1 databases and KV namespaces, auto-initialize the database schema via migrations, and present required secrets to users during deployment.

Purpose: This is the foundational configuration that makes zero-config one-click deployment possible. Without this, users would need to manually create D1 databases, run SQL files, and configure bindings.

Output: Clean wrangler.jsonc with empty resource IDs, D1 migration files, updated deploy script with migration chaining, .dev.vars.example with documented secrets, and fixed .gitignore.
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-configuration/04-RESEARCH.md
@wrangler.jsonc
@package.json
@.gitignore
@db/database.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean wrangler.jsonc and create D1 migration</name>
  <files>wrangler.jsonc, migrations/0001_initial_schema.sql</files>
  <action>
  **wrangler.jsonc changes:**

  This task assumes Phases 1-3 have already removed business-specific vars (FEISHU_CLIENT_ID, OAUTH_BASE_URL, IMAGE_PREFIX), internal routes, R2 CF_ALL_LOG bucket, and the test environment. The wrangler.jsonc at this point should already be partially cleaned. This task focuses on Deploy Button readiness:

  1. Ensure `database_id` in d1_databases is set to empty string `""` (for Deploy Button auto-provisioning)
  2. Add `"migrations_dir": "migrations"` to the d1_databases binding
  3. Ensure KV namespace `id` and `preview_id` are set to empty strings `""` (for Deploy Button auto-provisioning)
  4. Remove the `routes` section entirely (Deploy Button assigns its own workers.dev domain)
  5. Rename project from `qlj-devhub-homepage` to `devhub` (generic open-source name)
  6. Verify `vars` section only has open-source relevant variables (no business-specific ones should remain after Phase 1). If any business vars still exist, remove them.
  7. Remove the `env.test` section entirely (internal test environment not needed for open-source)
  8. Keep: `$schema`, `compatibility_date`, `compatibility_flags`, `main`, `upload_source_maps`, `observability`
  9. If ASSETS_BUCKET R2 binding still exists after Phase 1 cleanup, keep it for tool icon uploads. If it was removed, do not re-add it.

  **D1 migration file:**

  1. Create `migrations/` directory
  2. Create `migrations/0001_initial_schema.sql` containing CREATE TABLE IF NOT EXISTS statements for ALL application tables:
     - `tool_categories` (id, name, description, icon, color, created_at, updated_at)
     - `tools` (id, name, description, category, icon, is_internal, status, last_updated, created_at, updated_at, FK to tool_categories)
     - `tool_environments` (id autoincrement, tool_id, name, label, url, is_external, created_at, updated_at, FK to tools CASCADE)
     - `tool_tags` (id autoincrement, tool_id, tag, created_at, FK to tools CASCADE, UNIQUE tool_id+tag)
     - `tool_usage_events` (id, tool_id, used_at, FK to tools)
     - `users` table (id TEXT PK, username TEXT UNIQUE NOT NULL, password_hash TEXT NOT NULL, display_name TEXT, role TEXT NOT NULL DEFAULT 'user', created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL) -- from Phase 2/3
     - `sessions` table (id TEXT PK, user_id TEXT NOT NULL FK users CASCADE, refresh_token TEXT UNIQUE NOT NULL, expires_at INTEGER NOT NULL, created_at INTEGER NOT NULL) -- from Phase 2
     - `user_preferences` table (id TEXT PK, user_id TEXT NOT NULL, preferences_json TEXT NOT NULL DEFAULT '{}', updated_at INTEGER NOT NULL) -- for favorites/recent tools
  3. Include all indexes from existing schema: idx_tools_category, idx_tools_status, idx_tool_environments_tool_id, idx_tool_tags_tool_id, idx_tool_tags_tag, idx_tool_usage_tool_id, idx_tool_usage_used_at
  4. Add indexes for new tables: idx_sessions_user_id, idx_sessions_expires_at, idx_user_preferences_user_id
  5. Include seed data for default categories (generic English names instead of Chinese):
     - development: "Development Tools", "Code development and debugging tools", Code, #10b981
     - testing: "Testing Tools", "Automated testing and QA tools", TestTube, #c084fc
     - deployment: "Deployment Tools", "CI/CD and deployment management", Rocket, #f59f45
     - monitoring: "Monitoring Tools", "System monitoring and log analysis", BarChart, #ef4444
     - database: "Database Tools", "Database management and operations", Database, #f59f45
     - communication: "Collaboration Tools", "Team collaboration and communication", MessageSquare, #06b6d4
  6. Do NOT include business-specific tool data (the existing INSERT INTO tools/tool_environments/tool_tags rows contain internal company data)
  7. Use `CREATE TABLE IF NOT EXISTS` for all tables (idempotent)
  8. Add `PRAGMA defer_foreign_keys = true;` at the top of the migration

  The migration must represent the COMPLETE schema including tables added in Phases 2 and 3 (users, sessions, user_preferences). These tables should exist by the time Phase 4 executes since Phase 4 depends on Phase 3.
  </action>
  <verify>
    <automated>test -f migrations/0001_initial_schema.sql && grep -q "CREATE TABLE" migrations/0001_initial_schema.sql && grep -q 'database_id.*""' wrangler.jsonc && grep -q "migrations_dir" wrangler.jsonc && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>wrangler.jsonc has empty resource IDs, migrations_dir configured, no business routes/vars. migrations/0001_initial_schema.sql contains all table definitions with IF NOT EXISTS, indexes, and default category seed data.</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json, .dev.vars.example, and .gitignore</name>
  <files>package.json, .dev.vars.example, .gitignore</files>
  <action>
  **package.json changes:**

  1. Update `name` from `qlj-devhub-homepage` to `devhub`
  2. Update `description` to: "A self-hosted developer tool management platform. One-click deploy to Cloudflare Workers."
  3. Change `private` from `true` to `false` (required for public npm/open-source)
  4. Update the `deploy` script to chain migrations: `"deploy": "npm run build && wrangler d1 migrations apply DB --remote && wrangler deploy"`
  5. Add new migration scripts:
     - `"db:migrate"`: `"wrangler d1 migrations apply DB --remote"`
     - `"db:migrate:local"`: `"wrangler d1 migrations apply DB --local"`
  6. Remove the old `db:pull`, `db:reset`, `db:apply`, `db:sync` scripts (these reference the internal database name `devhub-database` directly and are internal workflow scripts)
  7. Keep the existing `db:setup` script if it was updated in earlier phases, or remove if it uses the old pattern
  8. Add `cloudflare.bindings` section with descriptions for Deploy Button UI:
     ```json
     "cloudflare": {
       "bindings": {
         "DB": {
           "type": "d1",
           "description": "Application database for tools, users, categories, and preferences"
         },
         "CACHE_KV": {
           "type": "kv",
           "description": "Cache storage for API responses (improves performance)"
         },
         "JWT_SECRET": {
           "type": "secret",
           "description": "Secret key for JWT token signing. Generate with: `openssl rand -hex 32`"
         }
       }
     }
     ```

  **.dev.vars.example file:**

  Create `.dev.vars.example` with documented secrets:
  ```
  # DevHub - Development Environment Variables
  # Copy this file to .dev.vars and fill in values

  # JWT signing secret for authentication tokens
  # Generate with: openssl rand -hex 32
  JWT_SECRET=your-jwt-secret-here
  ```

  Only include JWT_SECRET unless Phases 2/3 introduced additional secrets. Check the codebase for any `c.env.` references that are not defined in wrangler.jsonc vars or bindings -- those would be secrets that need documenting.

  **.gitignore update:**

  Add `!.dev.vars.example` negation after the `.dev.vars*` line so the example file is tracked by git:
  ```
  .dev.vars*
  !.dev.vars.example
  ```
  </action>
  <verify>
    <automated>grep -q "migrations apply" package.json && test -f .dev.vars.example && grep -q "JWT_SECRET" .dev.vars.example && grep -q '!.dev.vars.example' .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>package.json has updated deploy script with migration chaining, cloudflare.bindings descriptions, and generic project name. .dev.vars.example exists with documented JWT_SECRET. .gitignore excludes .dev.vars but tracks .dev.vars.example.</done>
</task>

</tasks>

<verification>
1. `grep -q 'database_id.*""' wrangler.jsonc` -- D1 database ID is empty for auto-provisioning
2. `grep -q "migrations_dir" wrangler.jsonc` -- migrations directory configured
3. `grep -q "migrations apply" package.json` -- deploy script chains migrations
4. `test -f migrations/0001_initial_schema.sql` -- migration file exists
5. `grep -c "CREATE TABLE" migrations/0001_initial_schema.sql` -- should return 8+ (all tables)
6. `test -f .dev.vars.example` -- secrets example file exists
7. `grep -q '!.dev.vars.example' .gitignore` -- example file not gitignored
8. `grep -qv "qiliangjia\|deepclick\|FEISHU\|OAUTH_BASE" wrangler.jsonc` -- no business-specific references
</verification>

<success_criteria>
- wrangler.jsonc is Deploy Button ready: empty resource IDs, no hardcoded routes/domains, migrations_dir configured
- migrations/0001_initial_schema.sql creates all application tables (tools, categories, environments, tags, usage, users, sessions, preferences) with indexes and default category seed data
- package.json deploy script chains: build -> migrate -> deploy
- .dev.vars.example documents JWT_SECRET with generation instructions
- .gitignore tracks .dev.vars.example while ignoring .dev.vars
- No business-specific references remain in deployment configuration files
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment-configuration/04-01-SUMMARY.md`
</output>
