---
phase: 04-deployment-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - README.md
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-05

must_haves:
  truths:
    - "README.md contains a visible Deploy to Cloudflare button badge linking to the public repo"
    - "The project exists as a public GitHub repository independent of the internal version"
    - "Clicking the Deploy Button URL opens the Cloudflare deployment flow"
  artifacts:
    - path: "README.md"
      provides: "Deploy Button badge and basic project description"
      contains: "deploy.workers.cloudflare.com"
  key_links:
    - from: "README.md Deploy Button"
      to: "Cloudflare Deploy flow"
      via: "deploy.workers.cloudflare.com/?url=REPO_URL"
      pattern: "deploy.workers.cloudflare.com"
---

<objective>
Create the public GitHub repository for DevHub and add a Deploy to Cloudflare button to the README, completing the one-click deployment chain.

Purpose: The Deploy Button is the visible entry point for zero-config deployment. Without a public repo and Deploy Button badge, users cannot discover or use one-click deployment.

Output: Public GitHub repository with Deploy Button badge in README.md.
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-configuration/04-RESEARCH.md
@.planning/phases/04-deployment-configuration/04-01-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Deploy Button to README and prepare repo metadata</name>
  <files>README.md</files>
  <action>
  Add a Deploy to Cloudflare button badge to the README.md file. Phase 5 (Documentation) will write comprehensive README content, so this task only adds the minimal Deploy Button section and project title.

  If README.md exists, add at the top (after any existing title):
  ```markdown
  # DevHub

  A self-hosted developer tool management platform. Deploy to Cloudflare Workers with one click.

  [![Deploy to Cloudflare](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/USERNAME/devhub)
  ```

  Replace `USERNAME` with the actual GitHub username/org where the repo will be created. Check the git remote origin to determine the correct username, or use a placeholder that the checkpoint task will confirm.

  If README.md does not exist, create it with the above content plus a brief description:
  ```markdown
  ## Features

  - Tool management with categories, tags, and multi-environment links
  - Command palette (Cmd/Ctrl+K) for quick navigation
  - Role-based access control (admin/user)
  - Built-in developer utilities (JSON formatter, Base64, URL parser)
  - Light/dark theme support
  - Edge-deployed on Cloudflare Workers for instant performance

  ## Quick Start

  Click the Deploy Button above, or deploy manually:

  ```bash
  git clone https://github.com/USERNAME/devhub.git
  cd devhub
  pnpm install
  cp .dev.vars.example .dev.vars  # Edit with your secrets
  pnpm run dev
  ```

  > Full documentation coming in Phase 5.
  ```

  Keep it minimal. Phase 5 will replace this with comprehensive documentation.
  </action>
  <verify>
    <automated>grep -q "deploy.workers.cloudflare.com" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md contains Deploy to Cloudflare button badge with link to the public repo URL.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create public GitHub repository</name>
  <what-built>All deployment configuration is complete (wrangler.jsonc, migrations, package.json, .dev.vars.example, README with Deploy Button). The project is ready to be pushed to a new public GitHub repository.</what-built>
  <how-to-verify>
  The user needs to create a public GitHub repository and push the code:

  1. **Create the repository** (via GitHub CLI or web UI):
     ```bash
     gh repo create devhub --public --description "A self-hosted developer tool management platform on Cloudflare Workers" --source=.
     ```
     Or create via https://github.com/new

  2. **Push the code** to the new repository:
     ```bash
     git remote add public https://github.com/YOUR_USERNAME/devhub.git
     git push public main
     ```

  3. **Update the Deploy Button URL** in README.md to use the actual repo URL:
     - Replace `https://github.com/USERNAME/devhub` with the actual URL
     - Commit and push the update

  4. **Verify the Deploy Button** by:
     - Opening the GitHub repo page
     - Clicking the "Deploy to Cloudflare" button in the README
     - Confirming it opens the Cloudflare deployment flow
     - Confirming D1 database and KV namespace bindings are shown for provisioning

  Note: This requires a GitHub account and cannot be automated by Claude since it involves authentication and repository ownership decisions.
  </how-to-verify>
  <resume-signal>Provide the public repository URL (e.g., https://github.com/username/devhub) or type "skip" to defer repository creation</resume-signal>
</task>

</tasks>

<verification>
1. README.md contains Deploy to Cloudflare button badge
2. Deploy Button URL points to `deploy.workers.cloudflare.com/?url=` with the public repo URL
3. Public GitHub repository exists and is accessible
4. Clicking Deploy Button opens Cloudflare deployment flow
</verification>

<success_criteria>
- README.md has a visible Deploy to Cloudflare button that links to the public repository
- The public GitHub repository exists at the specified URL
- The Deploy Button opens the Cloudflare Workers deployment flow when clicked
- The deployment flow shows D1 database and KV namespace bindings for auto-provisioning
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment-configuration/04-02-SUMMARY.md`
</output>
