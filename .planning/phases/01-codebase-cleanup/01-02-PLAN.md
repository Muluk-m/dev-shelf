---
phase: 01-codebase-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routes.ts
  - app/routes/tools/quick-login.tsx
  - app/routes/tools/roibest-analyzer.tsx
  - app/routes/tools/query-analyzer.tsx
  - app/routes/tools/query-analyzer.backup.tsx
  - app/routes/tools/website-check.tsx
  - app/routes/tools/whitelist-token.tsx
  - app/routes/tools/rb-domain-check.tsx
  - app/routes/tools/pwa-link-health.tsx
  - app/routes/tools/ab-router.tsx
  - app/routes/tools/cf-log-analyzer.tsx
  - app/routes/tools/pixel-activation-tool.tsx
  - app/components/ab-router/
  - app/components/query-analyzer/
  - app/components/pwa-link-health/
  - app/components/pixel-tools/
  - app/lib/clickhouse-service.ts
  - app/lib/pwa-link-health.ts
  - app/lib/query-templates.ts
  - app/lib/query-history-storage.ts
  - app/lib/whitelist-token.ts
  - app/lib/api.ts
  - app/types/ab-router.ts
  - app/types/cf-logs.ts
  - app/types/pwa-link-health.ts
  - app/types/roibest-analyzer.ts
  - app/types/website-check.ts
  - lib/types/cf-logs.ts
  - lib/types/query-analyzer.ts
  - app/routes/tools/json-diff.tsx
  - package.json
  - .npmrc
autonomous: true
requirements:
  - CLEAN-02
  - CLEAN-04
  - CLEAN-08

must_haves:
  truths:
    - "No business-specific frontend tool routes exist (quick-login, roibest, query-analyzer, website-check, etc.)"
    - "No business-specific component directories exist (ab-router, query-analyzer, pwa-link-health, pixel-tools)"
    - "No private NPM package dependency (@qlj/common-utils) exists in package.json or .npmrc"
    - "All generic tool routes still work (json-formatter, base64-converter, url-parser, etc.)"
    - "app/lib/api.ts contains only tool/category CRUD functions"
  artifacts:
    - path: "app/routes.ts"
      provides: "Clean route config with only generic tools"
      contains: "json-formatter"
    - path: "app/routes/tools/json-diff.tsx"
      provides: "Inline safeJsonParse replacing @qlj/common-utils"
      contains: "safeJsonParse"
    - path: "app/lib/api.ts"
      provides: "Clean API client with only tool/category functions"
  key_links:
    - from: "app/routes.ts"
      to: "app/routes/tools/json-formatter.tsx"
      via: "route definition"
      pattern: "json-formatter"
    - from: "app/routes/tools/json-diff.tsx"
      to: "safeJsonParse (inline)"
      via: "local function definition"
      pattern: "function safeJsonParse"
---

<objective>
Remove all business-specific frontend routes, their associated component directories, library files, and type files. Also eliminate the private @qlj/common-utils NPM package dependency by inlining the one function used from it.

Purpose: Strip all business-specific frontend code and private dependencies so the app contains only generic developer tools.
Output: Clean app/routes.ts with 10 generic tool routes, deleted 11 route files, 4 component dirs (~30 files), 5 lib files, 7 type files, removed private package.
</objective>

<execution_context>
@/Users/qian/.claude/get-shit-done/workflows/execute-plan.md
@/Users/qian/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-codebase-cleanup/01-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. -->

From app/routes.ts (CURRENT):
```typescript
// Routes to REMOVE:
route("pixel-activation-tool", "routes/tools/pixel-activation-tool.tsx"),
route("cf-log-analyzer", "routes/tools/cf-log-analyzer.tsx"),
route("query-analyzer", "routes/tools/query-analyzer.tsx"),
route("whitelist-token", "routes/tools/whitelist-token.tsx"),
route("website-check", "routes/tools/website-check.tsx"),
route("roibest-analyzer", "routes/tools/roibest-analyzer.tsx"),
route("rb-domain-check", "routes/tools/rb-domain-check.tsx"),
route("pwa-link-health", "routes/tools/pwa-link-health.tsx"),
route("quick-login", "routes/tools/quick-login.tsx"),
route("ab-router", "routes/tools/ab-router.tsx"),

// Routes to KEEP:
route("json-formatter", "routes/tools/json-formatter.tsx"),
route("base64-converter", "routes/tools/base64-converter.tsx"),
route("url-parser", "routes/tools/url-parser.tsx"),
route("url-encoder", "routes/tools/url-encoder.tsx"),
route("qr-generator", "routes/tools/qr-generator.tsx"),
route("time-formatter", "routes/tools/time-formatter.tsx"),
route("file-uploader", "routes/tools/file-uploader.tsx"),
route("json-diff", "routes/tools/json-diff.tsx"),
route("jwt-decoder", "routes/tools/jwt-decoder.tsx"),
route("ua-parser", "routes/tools/ua-parser.tsx"),
```

From app/routes/tools/json-diff.tsx line 1 (import to replace):
```typescript
import { safeJsonParse } from "@qlj/common-utils/common";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove business frontend routes, components, libs, and types</name>
  <files>
    app/routes.ts,
    app/routes/tools/quick-login.tsx,
    app/routes/tools/roibest-analyzer.tsx,
    app/routes/tools/query-analyzer.tsx,
    app/routes/tools/query-analyzer.backup.tsx,
    app/routes/tools/website-check.tsx,
    app/routes/tools/whitelist-token.tsx,
    app/routes/tools/rb-domain-check.tsx,
    app/routes/tools/pwa-link-health.tsx,
    app/routes/tools/ab-router.tsx,
    app/routes/tools/cf-log-analyzer.tsx,
    app/routes/tools/pixel-activation-tool.tsx,
    app/components/ab-router/,
    app/components/query-analyzer/,
    app/components/pwa-link-health/,
    app/components/pixel-tools/,
    app/lib/clickhouse-service.ts,
    app/lib/pwa-link-health.ts,
    app/lib/query-templates.ts,
    app/lib/query-history-storage.ts,
    app/lib/whitelist-token.ts,
    app/types/ab-router.ts,
    app/types/cf-logs.ts,
    app/types/pwa-link-health.ts,
    app/types/roibest-analyzer.ts,
    app/types/website-check.ts,
    lib/types/cf-logs.ts,
    lib/types/query-analyzer.ts
  </files>
  <action>
**DELETE 11 business-specific frontend route files:**
- `app/routes/tools/quick-login.tsx` (DeepClick quick login -- hardcoded internal URLs)
- `app/routes/tools/roibest-analyzer.tsx` (RoiBest link analyzer)
- `app/routes/tools/query-analyzer.tsx` (~1250 lines -- ClickHouse query tool)
- `app/routes/tools/query-analyzer.backup.tsx` (~1167 lines -- backup file)
- `app/routes/tools/website-check.tsx` (~1224 lines -- website checker)
- `app/routes/tools/whitelist-token.tsx` (contains hardcoded secret key)
- `app/routes/tools/rb-domain-check.tsx` (domain status checker)
- `app/routes/tools/pwa-link-health.tsx` (PWA link health checker)
- `app/routes/tools/ab-router.tsx` (A/B testing router UI)
- `app/routes/tools/cf-log-analyzer.tsx` (CF log analyzer UI)
- `app/routes/tools/pixel-activation-tool.tsx` (pixel activation tool)

**DELETE 4 business-specific component directories (entire directories):**
- `app/components/ab-router/` (10 files)
- `app/components/query-analyzer/` (7 files)
- `app/components/pwa-link-health/` (11 files)
- `app/components/pixel-tools/` (2 files)

**DELETE 5 business-specific lib files:**
- `app/lib/clickhouse-service.ts` (contains hardcoded access key `d561b95f5cda783b50042f9d75e912d3`)
- `app/lib/pwa-link-health.ts`
- `app/lib/query-templates.ts`
- `app/lib/query-history-storage.ts`
- `app/lib/whitelist-token.ts` (contains hardcoded secret key `c7d065b0d405ec46e2597c4e53368598`)

**DELETE 7 business-specific type files:**
- `app/types/ab-router.ts`
- `app/types/cf-logs.ts`
- `app/types/pwa-link-health.ts`
- `app/types/roibest-analyzer.ts`
- `app/types/website-check.ts`
- `lib/types/cf-logs.ts`
- `lib/types/query-analyzer.ts`

**UPDATE `app/routes.ts`** to remove route definitions for all 11 deleted routes. The result should be:
```typescript
import { index, type RouteConfig, route } from "@react-router/dev/routes";

export default [
  index("routes/home.tsx"),
  route("admin", "routes/admin.tsx"),
  route("admin/permissions", "routes/admin.permissions.tsx"),

  route("tools", "routes/tools/_layout.tsx", [
    route("json-formatter", "routes/tools/json-formatter.tsx"),
    route("base64-converter", "routes/tools/base64-converter.tsx"),
    route("url-parser", "routes/tools/url-parser.tsx"),
    route("url-encoder", "routes/tools/url-encoder.tsx"),
    route("qr-generator", "routes/tools/qr-generator.tsx"),
    route("time-formatter", "routes/tools/time-formatter.tsx"),
    route("file-uploader", "routes/tools/file-uploader.tsx"),
    route("json-diff", "routes/tools/json-diff.tsx"),
    route("jwt-decoder", "routes/tools/jwt-decoder.tsx"),
    route("ua-parser", "routes/tools/ua-parser.tsx"),
  ]),

  route("tools/:id", "routes/tools.$id.tsx"),
] satisfies RouteConfig;
```

**IMPORTANT:** The `query-analyzer.backup.tsx` file is tech debt -- delete it too. The hardcoded credentials in `clickhouse-service.ts` and `whitelist-token.ts` are security issues being resolved by deletion.
  </action>
  <verify>
    <automated>cd /Users/qian/Projects/Qlj/qlj-devhub-homepage && ! test -f app/routes/tools/quick-login.tsx && ! test -f app/routes/tools/query-analyzer.tsx && ! test -d app/components/ab-router && ! test -f app/lib/clickhouse-service.ts && ! test -f app/lib/whitelist-token.ts && grep -c "route(" app/routes.ts | grep -q "1[0-3]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- 11 business route files deleted
- 4 component directories deleted (~30 files)
- 5 lib files deleted (including 2 with hardcoded credentials)
- 7 type files deleted
- app/routes.ts contains only 10 generic tool routes + home + admin + tools/$id
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean app/lib/api.ts + inline safeJsonParse + remove @qlj/common-utils</name>
  <files>
    app/lib/api.ts,
    app/routes/tools/json-diff.tsx,
    package.json,
    .npmrc
  </files>
  <action>
**CLEAN `app/lib/api.ts`** (currently 841 lines). Remove ALL business-specific API functions:

Functions/sections to REMOVE:
- `getUserInfo()` and related auth functions (Feishu auth removed)
- `logout()` function (auth removed)
- All CF log functions: `listCfLogs()`, `queryCfLogs()`, and their type interfaces
- All query analyzer functions: `convertToSQL()`, `executeQuery()`, `analyzeData()`, and their interfaces
- All icon generator functions: `generateToolIcon()`, `GenerateIconRequest`, `GenerateIconResponse` interfaces
- All AB router functions (approximately 370 lines, roughly half the file): all functions under the AB router section and the `AB_ROUTER_UPSTREAM_URL` constant
- Any imports of deleted type files (`ab-router.ts`, `cf-logs.ts`, etc.)

Functions to KEEP:
- `API_BASE_URL` constant (but FIX the value -- address this: currently references `qiliangjia.one`. For now, keep the dynamic logic but remove the hardcoded production URL. Replace `"https://qlj-devhub-homepage.qiliangjia.one"` with `""` in the production case -- Plan 03 will handle final config)
- All tool CRUD functions: `fetchTools()`, `createTool()`, `updateTool()`, `deleteTool()`, etc.
- All category functions: `fetchCategories()`, `createCategory()`, etc.
- Upload functions: `uploadFile()`, etc.
- Any shared utility functions used by the kept functions

After cleaning, `api.ts` should be roughly 200-350 lines (down from 841).

**INLINE `safeJsonParse` in `app/routes/tools/json-diff.tsx`:**
Replace the import:
```typescript
// BEFORE:
import { safeJsonParse } from "@qlj/common-utils/common";

// AFTER: Add this function at the top of the file (after imports, before component)
function safeJsonParse(text: string): unknown {
  try {
    return JSON.parse(text);
  } catch {
    return null;
  }
}
```
Remove the `@qlj/common-utils` import line entirely.

**REMOVE `@qlj/common-utils` from `package.json`:**
Delete the line `"@qlj/common-utils": "^0.0.11",` from the `dependencies` section.

**DELETE `.npmrc`:**
The file contains `@qlj:registry=https://nexus.qiliangjia.com/repository/npm-local/` -- a private registry that won't be accessible to open-source users. Delete the entire file.

**NOTE:** Do NOT run `pnpm install` yet -- Plan 03 will handle the final clean install + lockfile regeneration after all config changes are done.
  </action>
  <verify>
    <automated>cd /Users/qian/Projects/Qlj/qlj-devhub-homepage && ! test -f .npmrc && ! grep -q "@qlj/common-utils" package.json && grep -q "safeJsonParse" app/routes/tools/json-diff.tsx && ! grep -q "AB_ROUTER_UPSTREAM_URL" app/lib/api.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- app/lib/api.ts reduced from ~841 lines to ~200-350 lines with only tool/category/upload functions
- json-diff.tsx uses inline safeJsonParse instead of @qlj/common-utils import
- @qlj/common-utils removed from package.json dependencies
- .npmrc private registry config file deleted
- No hardcoded internal URLs remain in api.ts (production URL replaced)
  </done>
</task>

</tasks>

<verification>
```bash
# Verify no business route files remain
for f in quick-login roibest-analyzer query-analyzer website-check whitelist-token rb-domain-check pwa-link-health ab-router cf-log-analyzer pixel-activation-tool; do
  test -f "app/routes/tools/$f.tsx" && echo "FAIL: $f still exists" || true
done

# Verify component directories deleted
for d in ab-router query-analyzer pwa-link-health pixel-tools; do
  test -d "app/components/$d" && echo "FAIL: $d dir still exists" || true
done

# Verify no private package references
grep -r "@qlj" package.json .npmrc 2>/dev/null && echo "FAIL: @qlj references remain" || echo "PASS: No @qlj references"

# Verify no hardcoded credentials in remaining code
grep -r "d561b95f5cda783b50042f9d75e912d3\|c7d065b0d405ec46e2597c4e53368598\|app-Uwz1jNDR" app/ lib/ --include="*.ts" --include="*.tsx" | grep -v ".planning/" && echo "FAIL: Hardcoded credentials remain" || echo "PASS: No credentials"
```
</verification>

<success_criteria>
1. All 11 business-specific frontend route files are deleted
2. All 4 business component directories are deleted (~30 files)
3. All 5 business lib files are deleted (including those with hardcoded credentials)
4. All 7 business type files are deleted
5. app/routes.ts has exactly 10 generic tool routes
6. app/lib/api.ts contains only tool/category/upload functions (~200-350 lines)
7. json-diff.tsx uses inline safeJsonParse
8. @qlj/common-utils removed from package.json, .npmrc deleted
</success_criteria>

<output>
After completion, create `.planning/phases/01-codebase-cleanup/01-02-SUMMARY.md`
</output>
